# mackerel-plugin-sslcert

Mackerel plugin to get number of days until SSL certificate expires.

## usage

```
[plugin.metrics.sslcert]
command = "/path/to/mackerel-plugin-sslcert /path/to/sslcert"
```
